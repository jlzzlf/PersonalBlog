---
import { getCollection } from 'astro:content';
import Banner from '../components/Banner.astro';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import HomeBlogShowcase from '../components/home/HomeBlogShowcase.astro';
import HomeProfileCard from '../components/home/HomeProfileCard.astro';
import HomeProjectShowcaseCard from '../components/home/HomeProjectShowcaseCard.astro';
import HomeQuickNavCard from '../components/home/HomeQuickNavCard.astro';
import HomeRecentCommentsCard from '../components/home/HomeRecentCommentsCard.astro';
import HomeRecentPostsCard from '../components/home/HomeRecentPostsCard.astro';
import HomeSiteStatsCard from '../components/home/HomeSiteStatsCard.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { buildHomePageViewModel } from '../lib/homepage';
import { buildHomeProjectShowcaseItems, sortProjectEntries } from '../lib/projects';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const projectEntries = sortProjectEntries(await getCollection('projects'));

const { recentPosts, siteStats } = buildHomePageViewModel(posts);
const projectShowcaseItems = buildHomeProjectShowcaseItems(projectEntries);
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body class="home-page" data-banner-mode="background">
		<Banner mode="background" />
		<Header />
		<main class="home" data-parallax-home>
			<section id="hero" class="home-section home-hero" aria-label="首页介绍">
				<HomeProfileCard />
				<HomeRecentPostsCard posts={recentPosts} />
			</section>

			<section id="projects" class="home-section projects-section" aria-labelledby="home-projects-title">
				<div class="section-heading section-heading--split home-reveal" data-reveal-card>
					<div>
						<p class="section-kicker">Project Showcase</p>
						<h2 id="home-projects-title">项目展示</h2>
					</div>
					<a class="text-link" href="/projects/">查看全部项目<span aria-hidden="true">→</span></a>
				</div>
				<HomeProjectShowcaseCard items={projectShowcaseItems} />
			</section>

			<section id="blog" class="home-section blog-section" aria-labelledby="home-blog-title">
				<div class="section-heading section-heading--split home-reveal" data-reveal-card>
					<div>
						<p class="section-kicker">Latest Articles</p>
						<h2 id="home-blog-title">博客文章</h2>
					</div>
					<a class="text-link" href="/blog/">查看全部文章<span aria-hidden="true">→</span></a>
				</div>
				<HomeBlogShowcase posts={recentPosts} />
			</section>

			<section id="links" class="home-section links-section" aria-label="快捷导航">
				<HomeQuickNavCard />
			</section>

			<section id="comments" class="home-section comments-section" aria-labelledby="home-comments-title">
				<HomeRecentCommentsCard />
			</section>

			<section class="home-section stats-section" aria-labelledby="home-stats-title">
				<div class="section-heading section-heading--center home-reveal" data-reveal-card>
					<p class="section-kicker">Site Statistics</p>
					<h2 id="home-stats-title">站点数据</h2>
				</div>
				<HomeSiteStatsCard stats={siteStats} />
			</section>
		</main>
		<Footer />
	</body>
</html>

<style is:global>
	.home {
		position: relative;
		z-index: 5;
		display: grid;
		gap: clamp(2.75rem, 5vw, 4.75rem);
		width: min(1360px, calc(100% - 2rem));
		margin: 0 auto;
		padding: clamp(7rem, 18vh, 10rem) 0 4.5rem;
		isolation: isolate;
	}

	body.home-page {
		--color-bg: #E6FFFB;
	}

	body.home-page[data-banner-mode='background'] main.home {
		padding: clamp(7rem, 18vh, 10rem) 0 4.5rem;
	}

	.home-section {
		position: relative;
		z-index: 1;
	}

	.home-hero {
		display: grid;
		grid-template-columns: minmax(0, 1.08fr) minmax(20rem, 26rem);
		gap: clamp(1.25rem, 3vw, 2.5rem);
		align-items: end;
		width: min(1180px, 100%);
		min-height: clamp(32rem, 62svh, 40rem);
		margin: 0 auto;
	}

	.home-card {
		position: relative;
		overflow: hidden;
		border: 1px solid rgba(204, 255, 255, 0.5);
		border-radius: 1.5rem;
		background: rgba(255, 255, 255, 0.7);
		box-shadow: 0 8px 32px rgba(102, 204, 204, 0.1);
		backdrop-filter: blur(20px);
		-webkit-backdrop-filter: blur(20px);
	}

	.home-card::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: 0;
		background: linear-gradient(135deg, rgba(204, 255, 255, 0.22), transparent 48%);
		opacity: 0;
		transition: opacity 500ms ease;
		pointer-events: none;
	}

	.home-card > * {
		position: relative;
		z-index: 1;
	}

	.home-card:hover::before {
		opacity: 1;
	}

	.home-reveal {
		opacity: 0;
		transform: translateY(30px);
		transition:
			opacity 700ms ease,
			transform 700ms ease,
			border-color 300ms ease,
			background-color 300ms ease,
			box-shadow 300ms ease;
		transition-delay: var(--reveal-delay, 0ms);
	}

	.home-reveal.is-visible {
		opacity: 1;
		transform: translateY(0);
	}

	.home-card.home-reveal:hover {
		transform: translateY(-5px);
		border-color: rgba(102, 204, 204, 0.6);
		box-shadow: 0 20px 40px rgba(102, 204, 204, 0.2);
	}

	.section-heading {
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 1rem;
		margin-bottom: 2rem;
	}

	.section-heading--center {
		display: grid;
		justify-items: center;
		text-align: center;
	}

	.section-heading--left {
		display: grid;
		justify-items: start;
		margin-bottom: 0.9rem;
	}

	.section-heading h2,
	.recent-card__header h2,
	.project-feature-card h3,
	.project-more-card h3,
	.blog-feature-card h3,
	.blog-regular-card h3 {
		color: #1a2b3c;
	}

	.section-kicker {
		margin: 0 0 0.45rem;
		color: #66cccc;
		font-size: 0.82rem;
		font-weight: 700;
		letter-spacing: 0.16em;
		text-transform: uppercase;
	}

	.text-link,
	.blog-card-link,
	.project-more-card__button {
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		color: #66cccc;
		font-size: 0.9rem;
		font-weight: 700;
		text-decoration: none;
		transition:
			color 220ms ease,
			transform 220ms ease;
	}

	.text-link:hover,
	.blog-card-link:hover,
	.project-more-card__button:hover {
		color: #339999;
	}

	.text-link:hover span,
	.blog-card-link:hover span,
	.project-more-card__button:hover span {
		transform: translateX(0.2rem);
	}

	.text-link span,
	.blog-card-link span,
	.project-more-card__button span {
		transition: transform 220ms ease;
	}

	.profile-card {
		display: grid;
		gap: clamp(1.35rem, 2.4vw, 1.9rem);
		align-content: center;
	}

	.profile-copy {
		display: grid;
		gap: clamp(1.35rem, 2.5vw, 2rem);
		max-width: 36rem;
	}

	.profile-heading {
		display: grid;
		gap: 0.42rem;
	}

	.profile-kicker {
		margin: 0;
		color: #66cccc;
		font-size: 0.9rem;
		font-weight: 700;
		letter-spacing: 0.16em;
		text-transform: uppercase;
	}

	.profile-name {
		margin: 0;
		color: #071522;
		font-size: clamp(64px, 8vw, 6.8rem);
		font-weight: 800;
		line-height: 0.96;
		letter-spacing: 1px;
		text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
	}

	.profile-role {
		margin: 0;
		color: rgba(20, 40, 50, 0.7);
		font-size: 1.2rem;
		font-weight: 400;
		text-shadow: 0 1px 3px rgba(0, 0, 0, 0.15);
	}

	.profile-intro {
		display: grid;
		gap: 0.35rem;
	}

	.profile-intro p {
		margin: 0;
		color: #4a5a6a;
		font-size: 1.05rem;
		line-height: 1.75;
	}

	.profile-tags,
	.blog-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.65rem;
	}

	.profile-tags {
		margin-top: 0.1rem;
	}

	.profile-tags span {
		display: inline-flex;
		align-items: center;
		padding: 0.48rem 0.95rem;
		border-radius: 999px;
		background: linear-gradient(135deg, #66cccc, #99e6e6);
		color: #1a6666;
		font-size: 0.86rem;
		font-weight: 700;
		box-shadow: 0 10px 20px rgba(102, 204, 204, 0.16);
		transition:
			transform 300ms ease,
			box-shadow 300ms ease;
	}

	.profile-tags span:hover {
		transform: translateY(-5px);
		box-shadow: 0 20px 40px rgba(102, 204, 204, 0.2);
	}

	.profile-contacts {
		display: grid;
		gap: 0.55rem;
		padding-top: 0.1rem;
	}

	.profile-contact {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.7rem;
		margin: 0;
		color: #4a5a6a;
		font-size: 0.95rem;
	}

	.profile-contact span {
		color: #7a8a9a;
	}

	.profile-contact__copy {
		padding: 0;
		border: 0;
		background: transparent;
		color: #1a2b3c;
		font: inherit;
		cursor: pointer;
		transition:
			color 180ms ease,
			opacity 180ms ease;
	}

	.profile-contact__copy:hover,
	.profile-contact__copy.is-copied {
		color: #66cccc;
	}

	.profile-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.85rem;
		margin-top: -0.25rem;
	}

	.profile-action {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 3rem;
		height: 3rem;
		border: 1px solid rgba(204, 255, 255, 0.5);
		border-radius: 0.85rem;
		background: rgba(255, 255, 255, 0.7);
		box-shadow: 0 8px 32px rgba(102, 204, 204, 0.1);
		text-decoration: none;
		transition:
			transform 300ms ease,
			background-color 300ms ease,
			box-shadow 300ms ease;
	}

	.profile-action:hover {
		transform: translateY(-5px);
		background: rgba(204, 255, 255, 0.4);
		box-shadow: 0 20px 40px rgba(102, 204, 204, 0.2);
	}

	.profile-action-image {
		width: 1.3rem;
		height: 1.3rem;
		object-fit: contain;
	}

	.profile-action-image.is-leetcode {
		transform: translateX(-0.12rem);
	}

	.recent-card {
		display: grid;
		align-self: center;
		gap: 1.45rem;
		margin-top: clamp(3rem, 9vh, 6.5rem);
		padding: clamp(1.35rem, 2.4vw, 2rem);
		border-color: rgba(153, 230, 230, 0.68);
		border-radius: 1.75rem;
		box-shadow:
			0 20px 40px rgba(0, 0, 0, 0.12),
			0 0 34px rgba(204, 255, 255, 0.28);
	}

	.recent-card__avatar {
		display: flex;
		align-items: center;
		gap: 1rem;
	}

	.recent-card__avatar-frame {
		width: 4rem;
		height: 4rem;
		flex: 0 0 auto;
		padding: 0.12rem;
		border-radius: 1rem;
		background: linear-gradient(135deg, #ccffff, #99e6e6, #66cccc);
	}

	.recent-card__avatar-image {
		width: 100%;
		height: 100%;
		border-radius: 0.9rem;
		object-fit: cover;
	}

	.recent-card__avatar p {
		margin: 0 0 0.2rem;
		color: #1a2b3c;
		font-weight: 700;
		line-height: 1.45;
	}

	.recent-card__avatar span {
		color: #7a8a9a;
		font-size: 0.85rem;
	}

	.recent-card__header {
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 1rem;
	}

	.recent-card__header h2 {
		margin: 0;
		font-size: clamp(1.6rem, 3vw, 2rem);
	}

	.recent-list {
		display: grid;
		gap: 1rem;
	}

	.recent-item {
		display: grid;
		gap: 0.45rem;
		padding: 1rem;
		border: 1px solid rgba(204, 255, 255, 0.3);
		border-radius: 1.2rem;
		background: rgba(255, 255, 255, 0.5);
		color: inherit;
		text-decoration: none;
		transition:
			transform 300ms ease,
			border-color 300ms ease,
			background-color 300ms ease,
			box-shadow 300ms ease;
	}

	.recent-item:hover {
		transform: translateY(-2px);
		border-color: rgba(102, 204, 204, 0.5);
		background: rgba(204, 255, 255, 0.3);
		box-shadow: 0 16px 30px rgba(102, 204, 204, 0.12);
	}

	.recent-item__meta,
	.blog-card-meta {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.75rem;
		color: #7a8a9a;
		font-size: 0.85rem;
		font-weight: 700;
	}

	.recent-item__meta {
		justify-content: space-between;
	}

	.recent-item__meta strong {
		padding: 0.22rem 0.55rem;
		border-radius: 999px;
		background: rgba(204, 255, 255, 0.5);
		color: #339999;
		font-size: 0.72rem;
	}

	.recent-item__body {
		display: grid;
		gap: 0.25rem;
	}

	.recent-item__body strong,
	.blog-regular-card h3 {
		color: #1a2b3c;
		line-height: 1.45;
		transition: color 300ms ease;
	}

	.recent-item:hover .recent-item__body strong,
	.blog-regular-card:hover h3,
	.blog-feature-card:hover h3,
	.project-feature-card:hover h3,
	.quick-nav-tile:hover strong {
		color: #66cccc;
	}

	.recent-item__body p,
	.project-feature-card p,
	.project-more-card p,
	.blog-feature-card p,
	.blog-regular-card p,
	.quick-nav-copy,
	.quick-nav-tile__body p {
		margin: 0;
		color: #4a5a6a;
		line-height: 1.68;
	}

	.stats-grid {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: 1.05rem;
		margin: 0;
	}

	.stat-card {
		display: flex;
		align-items: center;
		gap: 1rem;
		min-height: 8.6rem;
		padding: 1.4rem;
		border-radius: 1.25rem;
	}

	.stat-card dt {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin: 0;
		color: #7a8a9a;
		font-size: 0.86rem;
	}

	.stat-card dd {
		margin: 0.2rem 0 0;
		color: #1a2b3c;
		font-size: 2rem;
		font-weight: 800;
		line-height: 1.1;
		font-variant-numeric: tabular-nums;
	}

	.stat-card dd small {
		margin-left: 0.25rem;
		color: #66cccc;
		font-size: 1rem;
	}

	.stat-card__icon,
	.project-feature-card__icon,
	.project-more-card__icon,
	.blog-feature-card__icon,
	.quick-nav-thumb {
		display: inline-grid;
		place-items: center;
		border-radius: 0.9rem;
		background: linear-gradient(135deg, #66cccc, #99e6e6);
		color: #fff;
		box-shadow: 0 12px 24px rgba(102, 204, 204, 0.2);
	}

	.stat-card__icon {
		width: 3.5rem;
		height: 3.5rem;
		flex: 0 0 auto;
		font-size: 1.05rem;
		font-weight: 800;
	}

	.project-showcase-grid {
		display: grid;
		grid-template-columns: minmax(0, 2fr) minmax(18rem, 1fr);
		gap: 1.05rem;
	}

	.project-feature-card,
	.project-more-card,
	.blog-feature-card,
	.blog-regular-card {
		display: grid;
		align-content: space-between;
		padding: clamp(1.35rem, 2.4vw, 2rem);
	}

	.project-feature-card {
		min-height: 20rem;
		border-radius: 1.75rem;
		background:
			linear-gradient(135deg, rgba(102, 204, 204, 0.16), rgba(153, 230, 230, 0.14)),
			rgba(255, 255, 255, 0.7);
	}

	.project-feature-card__meta,
	.project-feature-card__actions,
	.blog-regular-card__footer,
	.project-more-card__header,
	.quick-nav-tile__top {
		display: flex;
		align-items: center;
		gap: 0.9rem;
	}

	.project-feature-card__meta {
		justify-content: space-between;
	}

	.project-feature-card__meta span:first-child {
		display: inline-flex;
		padding: 0.35rem 0.8rem;
		border-radius: 999px;
		background: rgba(204, 255, 255, 0.5);
		color: #339999;
		font-size: 0.82rem;
		font-weight: 700;
	}

	.project-feature-card__type {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.42rem 0.86rem;
		border: 1px solid rgba(102, 204, 204, 0.28);
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.62);
		color: #1a6666;
		font-size: 0.76rem;
		font-weight: 900;
		letter-spacing: 0.08em;
		line-height: 1.1;
		text-transform: uppercase;
		box-shadow: 0 10px 20px rgba(102, 204, 204, 0.12);
	}

	.project-feature-card__icon,
	.project-more-card__icon,
	.blog-feature-card__icon {
		width: 3rem;
		height: 3rem;
		font-size: 0.78rem;
		font-weight: 800;
		letter-spacing: 0.04em;
	}

	.project-feature-card h3,
	.blog-feature-card h3 {
		margin: 0 0 0.75rem;
		font-size: clamp(1.6rem, 3vw, 2rem);
	}

	.project-feature-card__keywords {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		margin-top: 1rem;
	}

	.project-feature-card__keywords span {
		display: inline-flex;
		align-items: center;
		padding: 0.32rem 0.72rem;
		border: 1px solid rgba(153, 230, 230, 0.46);
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.68);
		color: #247474;
		font-size: 0.78rem;
		font-weight: 800;
		line-height: 1.2;
	}

	.project-feature-card__actions {
		flex-wrap: wrap;
	}

	.button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 3rem;
		padding: 0.8rem 1.2rem;
		border-radius: 0.9rem;
		font-weight: 800;
		text-decoration: none;
		transition:
			transform 300ms ease,
			box-shadow 300ms ease,
			background-color 300ms ease;
	}

	.button:hover {
		transform: translateY(-2px);
	}

	.button-primary {
		border: 1px solid transparent;
		background: linear-gradient(135deg, #66cccc, #99e6e6);
		color: #1a6666;
		box-shadow: 0 14px 28px rgba(102, 204, 204, 0.18);
	}

	.button-primary:hover {
		color: #1a6666;
		box-shadow: 0 18px 34px rgba(102, 204, 204, 0.3);
	}

	.button-secondary {
		border: 1px solid rgba(204, 255, 255, 0.5);
		background: rgba(255, 255, 255, 0.7);
		color: #4a5a6a;
	}

	.button-secondary:hover {
		color: #1a2b3c;
		background: rgba(204, 255, 255, 0.4);
	}

	.project-more-card {
		min-height: 20rem;
		gap: 1rem;
	}

	.project-more-card__header {
		align-items: flex-start;
	}

	.project-more-card__header h3 {
		margin: 0;
		font-size: 1.35rem;
	}

	.project-more-card__list {
		display: grid;
		gap: 0.7rem;
	}

	.project-more-card__item {
		display: flex;
		align-items: center;
		gap: 0.7rem;
		padding: 0.75rem 0.85rem;
		border-radius: 0.9rem;
		background: rgba(204, 255, 255, 0.2);
		color: #4a5a6a;
		text-decoration: none;
		transition:
			color 240ms ease,
			background-color 240ms ease;
	}

	.project-more-card__item:hover {
		background: rgba(204, 255, 255, 0.4);
		color: #1a2b3c;
	}

	.project-more-card__empty {
		padding: 0.9rem 1rem;
		border: 1px dashed rgba(153, 230, 230, 0.58);
		border-radius: 1rem;
		background: rgba(255, 255, 255, 0.42);
		font-size: 0.92rem;
	}

	.blog-showcase-grid {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
		gap: 1.05rem;
	}

	.blog-feature-card {
		min-height: 28rem;
		background:
			linear-gradient(135deg, rgba(204, 255, 255, 0.3), transparent 44%, rgba(153, 230, 230, 0.2)),
			rgba(255, 255, 255, 0.7);
	}

	.blog-feature-card__badge {
		display: inline-flex;
		width: fit-content;
		padding: 0.32rem 0.78rem;
		border-radius: 999px;
		background: linear-gradient(135deg, #66cccc, #99e6e6);
		color: #1a6666;
		font-size: 0.78rem;
		font-weight: 800;
	}

	.blog-card-meta span::before {
		content: '·';
		margin-right: 0.75rem;
		color: #ccffff;
	}

	.blog-tags span {
		display: inline-flex;
		align-items: center;
		padding: 0.28rem 0.72rem;
		border-radius: 999px;
		background: rgba(204, 255, 255, 0.4);
		color: #339999;
		font-size: 0.8rem;
		font-weight: 700;
	}

	.blog-regular-list {
		display: grid;
		gap: 1.05rem;
	}

	.blog-regular-card {
		min-height: 13.45rem;
	}

	.blog-regular-card h3 {
		margin: 0;
		font-size: 1.25rem;
	}

	.blog-regular-card__footer {
		justify-content: space-between;
		align-items: flex-end;
		gap: 1rem;
	}

	.blog-card-link.is-icon {
		flex: 0 0 auto;
		justify-content: center;
		width: 2.3rem;
		height: 2.3rem;
		border-radius: 999px;
		background: rgba(204, 255, 255, 0.24);
	}

	.latest-comments-card {
		display: grid;
		gap: 1.25rem;
		padding: clamp(1.35rem, 2.4vw, 2rem);
		border-radius: 1.75rem;
		background:
			linear-gradient(135deg, rgba(204, 255, 255, 0.22), transparent 48%, rgba(153, 230, 230, 0.14)),
			rgba(255, 255, 255, 0.72);
		box-shadow:
			0 18px 42px rgba(0, 0, 0, 0.08),
			0 0 30px rgba(204, 255, 255, 0.22);
	}

	.latest-comments-card__header {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 1rem;
	}

	.latest-comments-card__header h2 {
		margin: 0;
		color: #1a2b3c;
		font-size: clamp(1.6rem, 3vw, 2rem);
	}

	.latest-comments-copy {
		max-width: 42rem;
		margin: 0.55rem 0 0;
		color: #4a5a6a;
		line-height: 1.68;
	}

	.latest-comments-card__action {
		flex: 0 0 auto;
	}

	.latest-comments-status,
	.latest-comments-empty {
		padding: 1rem 1.1rem;
		border: 1px solid rgba(153, 230, 230, 0.36);
		border-radius: 1.15rem;
		background: rgba(255, 255, 255, 0.64);
		color: #4a5a6a;
		line-height: 1.65;
	}

	.latest-comments-list {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
	}

	.latest-comment-item {
		display: grid;
		gap: 0.9rem;
		min-height: 11.5rem;
		padding: 1rem;
		border: 1px solid rgba(204, 255, 255, 0.36);
		border-radius: 1.2rem;
		background: rgba(255, 255, 255, 0.58);
		color: inherit;
		text-decoration: none;
		transition:
			transform 300ms ease,
			border-color 300ms ease,
			background-color 300ms ease,
			box-shadow 300ms ease;
	}

	.latest-comment-item:hover {
		transform: translateY(-3px);
		border-color: rgba(102, 204, 204, 0.52);
		background: rgba(204, 255, 255, 0.3);
		box-shadow: 0 16px 32px rgba(102, 204, 204, 0.14);
	}

	.latest-comment-item__meta {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 0.85rem;
	}

	.latest-comment-item__author {
		display: flex;
		align-items: center;
		gap: 0.75rem;
		min-width: 0;
	}

	.latest-comment-item__avatar {
		width: 2.65rem;
		height: 2.65rem;
		flex: 0 0 auto;
		border-radius: 999px;
		object-fit: cover;
		box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
	}

	.latest-comment-item__author-copy {
		display: grid;
		gap: 0.12rem;
		min-width: 0;
	}

	.latest-comment-item__author-copy strong {
		color: #1a2b3c;
		font-size: 0.95rem;
		line-height: 1.25;
	}

	.latest-comment-item__author-copy span {
		color: #7a8a9a;
		font-size: 0.78rem;
		font-weight: 700;
		line-height: 1.35;
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.latest-comment-item__time {
		flex: 0 0 auto;
		color: #7a8a9a;
		font-size: 0.78rem;
		font-weight: 800;
		white-space: nowrap;
	}

	.latest-comment-item__body {
		display: grid;
		gap: 0.75rem;
		align-content: space-between;
	}

	.latest-comment-item__body p {
		margin: 0;
		color: #4a5a6a;
		font-size: 0.92rem;
		line-height: 1.68;
	}

	.latest-comment-item__tag {
		display: inline-flex;
		width: fit-content;
		padding: 0.24rem 0.62rem;
		border-radius: 999px;
		background: rgba(204, 255, 255, 0.45);
		color: #339999;
		font-size: 0.74rem;
		font-weight: 800;
	}

	.quick-nav-card {
		display: grid;
		gap: 0.9rem;
	}

	.quick-nav-copy {
		max-width: 40rem;
	}

	.quick-nav-grid {
		display: grid;
		grid-template-columns: repeat(6, minmax(0, 1fr));
		gap: 1rem;
		margin-top: 0.6rem;
	}

	.quick-nav-tile {
		display: grid;
		gap: 0.8rem;
		min-height: 11rem;
		padding: 1.15rem;
		border-radius: 1.25rem;
		text-decoration: none;
	}

	.quick-nav-tile__top {
		align-items: flex-start;
	}

	.quick-nav-thumb {
		width: 2.65rem;
		height: 2.65rem;
		flex: 0 0 auto;
		overflow: hidden;
		font-size: 0.82rem;
		font-weight: 900;
	}

	.quick-nav-thumb img {
		width: 100%;
		height: 100%;
		padding: 0.45rem;
		object-fit: contain;
	}

	.quick-nav-tile__head {
		display: grid;
		gap: 0.15rem;
		min-width: 0;
	}

	.quick-nav-tile__head strong {
		color: #1a2b3c;
		font-size: 0.98rem;
		line-height: 1.25;
		transition: color 300ms ease;
	}

	.quick-nav-tile__head small {
		color: #7a8a9a;
		font-size: 0.72rem;
		font-weight: 700;
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
	}

	.quick-nav-tile__body p {
		font-size: 0.86rem;
	}

	.quick-nav-tile.is-pink .quick-nav-thumb {
		background: linear-gradient(135deg, #f7b2d0, #ffd6e8);
	}

	.quick-nav-tile.is-sky .quick-nav-thumb {
		background: linear-gradient(135deg, #5dade2, #99e6ff);
	}

	.quick-nav-tile.is-yellow .quick-nav-thumb {
		background: linear-gradient(135deg, #f39c12, #fff4cc);
	}

	@media (max-width: 1180px) {
		.quick-nav-grid {
			grid-template-columns: repeat(3, minmax(0, 1fr));
		}

		.stats-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}

	@media (max-width: 980px) {
		.home-hero,
		.project-showcase-grid,
		.blog-showcase-grid,
		.latest-comments-list {
			grid-template-columns: 1fr;
		}

		.home-hero {
			min-height: 0;
		}

		.recent-card {
			align-self: stretch;
			margin-top: 0;
		}
	}

	@media (max-width: 720px) {
		.home {
			width: min(44rem, calc(100% - 1rem));
			gap: 3.1rem;
			padding-top: 8rem;
		}

		.home-card.home-reveal:hover,
		.profile-tags span:hover,
		.profile-action:hover {
			transform: translateY(-2px);
		}

		.section-heading,
		.section-heading--split,
		.recent-card__header,
		.latest-comments-card__header {
			display: grid;
			justify-items: start;
		}

		.profile-name {
			font-size: clamp(3rem, 17vw, 4.35rem);
		}

		.profile-role {
			font-size: 1rem;
		}

		.recent-card,
		.project-feature-card,
		.project-more-card,
		.blog-feature-card,
		.blog-regular-card,
		.latest-comments-card {
			padding: 1.1rem;
		}

		.stats-grid,
		.quick-nav-grid {
			grid-template-columns: 1fr;
		}

		.stat-card {
			min-height: 7.4rem;
		}

		.project-feature-card__actions {
			display: grid;
			grid-template-columns: 1fr;
		}

		.button {
			width: 100%;
		}

		.blog-card-meta span::before {
			margin-right: 0.5rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.home-reveal,
		.home-card,
		.profile-tags span,
		.profile-action,
		.recent-item,
		.button,
		.text-link span,
		.blog-card-link span,
		.project-more-card__button span {
			transition: none;
		}

		.home-reveal {
			opacity: 1;
			transform: none;
		}
	}
</style>

<script>
	import '../scripts/home-page.ts';
</script>
